{% extends 'basedashboard.html.twig' %}
{% block title %}Edit Reclamation{% endblock %}

{% block body %}
    <h1>Edit Reclamation</h1>

    {{ include('rec/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('rec_index') }}">back to list</a>

    {{ include('rec/_delete_form.html.twig') }}
{% endblock body %}